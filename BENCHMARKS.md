# BENCHMARKS.md - the #17 headline metrics, measured and gated

- generated by `python3 scripts/run_benchmarks.py` (profile: full) <!-- @timing -->
- date 2026-09-19T12:42:56Z | host Linux-6.1.158+ | Python 3.11.2 | 2 vCPU <!-- @timing -->
- windows: n=60,000 (B1-B3, B5 policy), n=30,000 (B4), n=20,000 (B5 world) 
- reproduce: `python3 scripts/run_benchmarks.py --check` regenerates the full profile and diffs every line except those marked `<!-- @timing -->` (host timing, date).

The four headline definitions in one paragraph each, then the evidence; ADR-0016 (`docs/decisions/0016-benchmarks.md`) is the normative statement of the same content. **X** ([B1]): the auth-points concession and margin ratio of cost-blend static routing vs the shipped learned policy, measured pair-wise on one frontier per scenario - never quoted alone. **Y** ([B2]): 2 ms p99 decision wall at 5,000 d/s on 4 vCPU plus 20 us p99 in-engine CPU; measured today on the CPython reference (masked rows), projected as a labelled MODEL to the native band, PENDING for Go. **Z** ([B3]): oracle regret = (oracle - shipped)/oracle with the oracle = true rates through the identical execution loop, plus the clairvoyant bound that prices the score functional itself. **W** ([B4]): the OPE replay gate - IPS-clip10 vs replayed truth under support >= 1,000 and |err| <= max(1,500 c/1k, 75%|delta|) at N >= 30k.

House rules enforced here: no fabricated numbers (unmeasurable rows are PENDING with the gate armed); every number cites scenario@sha256:12 plus the world/policy seed pair; synthetic magnitudes are scenario properties, never production claims; margin rows carry MAE and arms-touched.

## B0 - scenario catalogue and identity pins

Every number in this file is a property of one of these worlds; the world is part of the number. `seed` is the world seed (in the document); `hash` is the canonical content hash the results cite. The catalogue itself is versioned: ids carry `-v1`, and a content change is a new id plus a reviewable diff in `simulator/scenarios/golden/`.

| scenario (versioned id) | model | seed | duration s | events | sha256:12 |
|---|---|---|---|---|---|
| baseline-steady-v1 | fleet-v1 | 20260915 | 604,800 | 0 | b49193b7e715 |
| black-friday-degraded-v1 | fleet-v1 | 20260915 | 604,800 | 4 | 89c669ee7371 |
| idempotency-stress-v1 | fleet-v1 | 20260915 | 3,600 | 5 | 984db10a2af0 |
| outage-recovery-v1 | fleet-v1 | 20260915 | 604,800 | 6 | 1e0cd5cd764e |
| replay-trace-v1 | fleet-v1 | 20260915 | 604,800 | 0 | abdba21ec1e9 |

* acquirer catalog: `acquirer-catalog.example.json` (6 acquirers), canonical pin `sha256:78b918123635` -> scenario docs declare the same pin: PASS
* determinism contract (splitmix64-indexed-v1) golden vectors: PASS
* golden scenario pins vs documents on disk: PASS
* seed identities: world seeds in the documents above; POLICY_SEED=20260916 (policy draws); OPE_SEED=20260920 (OPE replay draws). The three are separate by construction (ADR-0005 R30), so no number below can be a seed collision.

## B1 - the cost frontier: what auth costs in margin (the X pair)

The question the spec's `+X` asks: *does the learned router buy margin at the cost of authorization rate?* Method: the shipped policy against three named static routers, no strawmen:

  * **static MDR-only** - 'cost-based routing' as the industry ships it: sort by the processor's quoted rate (`cost_bps`), blind to fixed fees, attempt fees and auth rates;
  * **static cost-aware** - sort by the attempt's fully-loaded cost (`cost_bps*amount/1e4 + fixed_fee + attempt_fee`): the spreadsheet router done properly;
  * **static EV-table** - rank by E[margin of one attempt] from a 2,000-transaction uniform calibration prefix the run never sees (`theta_static` per acquirer; the same discipline as the TS policy's own offline prior), with no learning in the window.

All three take the two best-ranked eligible arms, depth 2, and route EVERYTHING - a static config has no statistical basis for refusing a transaction, so the refusal decision is precisely what the learned policy adds, and the table lets that difference speak. All policies see the same world, the same margins, ADR-0002's reward rules, and the identical execution loop.

Definitions: `auth%` = authorized transactions / transactions; `margin c/1k` = total ADR-0002 margin per 1,000 transactions; `C/auth c` = merchant effective cost per authorized transaction (acquirer MDR + fixed fees on authorized attempts, attempt fees on failed attempts, lambda_to x timeouts); `refused%` = transactions the policy would not route (shipped: every score <= 0; static: eligibility only); `MAE pts` = traffic-weighted |posterior mean - true rate| (shipped row only: static rows carry no posterior); `arms` = distinct acquirers attempted.

### baseline-steady-v1@sha256:b49193b7e715  (n=60,000; world seed 20260915, policy seed 20260916)

| policy | auth% | margin c/1k | C/auth c | refused% | MAE pts | arms |
|---|---|---|---|---|---|---|
| static MDR-only (industry cost routing) | 86.66 | 14,787.7 | 75.0 | 0.0 | - | 5 |
| static cost-aware (fully-loaded cost) | 89.56 | 26,206.9 | 62.0 | 0.0 | - | 5 |
| static EV-table (calibrated, no learning) | 89.54 | 26,086.6 | 62.1 | 0.0 | - | 5 |
| **shipped TS (learned)** | **70.49** | **27,376.2** | **73.4** | **20.6** | **4.25** | 5 |

**the pair, both ways**: against quoted-rate cost routing, the learned policy concedes **+16.17 auth points** (70.49 vs 86.66) and returns **x1.85 the margin** (27,376.2 vs 14,787.7 c/1k) at effective cost 73.4 vs 75.0 c/auth (-1.59 c - equal-or-better). Against the strongest calibrated static row (static cost-aware), the concession is +19.07 pts for x1.04. The concession itself is priced: the statics attempt the 20.6% of transactions the EV gate declines (negative-expectation micro-tickets), and the margin column is what that traffic is worth.

### black-friday-degraded-v1@sha256:89c669ee7371  (n=60,000; world seed 20260915, policy seed 20260916)

| policy | auth% | margin c/1k | C/auth c | refused% | MAE pts | arms |
|---|---|---|---|---|---|---|
| static MDR-only (industry cost routing) | 84.79 | 11,000.5 | 79.1 | 0.0 | - | 5 |
| static cost-aware (fully-loaded cost) | 88.42 | 23,517.1 | 64.5 | 0.0 | - | 5 |
| static EV-table (calibrated, no learning) | 88.48 | 23,543.1 | 64.5 | 0.0 | - | 5 |
| **shipped TS (learned)** | **67.82** | **25,305.8** | **77.1** | **22.6** | **4.29** | 5 |

**the pair, both ways**: against quoted-rate cost routing, the learned policy concedes **+16.97 auth points** (67.82 vs 84.79) and returns **x2.30 the margin** (25,305.8 vs 11,000.5 c/1k) at effective cost 77.1 vs 79.1 c/auth (-2.05 c - equal-or-better). Against the strongest calibrated static row (static EV-table), the concession is +20.66 pts for x1.07. The concession itself is priced: the statics attempt the 22.6% of transactions the EV gate declines (negative-expectation micro-tickets), and the margin column is what that traffic is worth.

### outage-recovery-v1@sha256:1e0cd5cd764e  (n=60,000; world seed 20260915, policy seed 20260916)

| policy | auth% | margin c/1k | C/auth c | refused% | MAE pts | arms |
|---|---|---|---|---|---|---|
| static MDR-only (industry cost routing) | 86.53 | 14,763.5 | 75.0 | 0.0 | - | 5 |
| static cost-aware (fully-loaded cost) | 89.53 | 26,181.8 | 62.1 | 0.0 | - | 5 |
| static EV-table (calibrated, no learning) | 89.50 | 26,056.0 | 62.1 | 0.0 | - | 5 |
| **shipped TS (learned)** | **70.41** | **27,353.1** | **73.4** | **20.6** | **4.29** | 5 |

**the pair, both ways**: against quoted-rate cost routing, the learned policy concedes **+16.12 auth points** (70.41 vs 86.53) and returns **x1.85 the margin** (27,353.1 vs 14,763.5 c/1k) at effective cost 73.4 vs 75.0 c/auth (-1.59 c - equal-or-better). Against the strongest calibrated static row (static cost-aware), the concession is +19.11 pts for x1.04. The concession itself is priced: the statics attempt the 20.6% of transactions the EV gate declines (negative-expectation micro-tickets), and the margin column is what that traffic is worth.

The frontier is the anti-cherry-pick: the delta-auth and the margin ratio are measured on the SAME line of the SAME table, and every row of the family is published - a reader does not have to trust that the comparison policy was a fair one, they can see the whole family.

## B2 - decision latency (Y): what the routing decision costs

Y has two halves, both ADR-0001 commitments restated here as the benchmark: (i) **2 ms p99 wall** per routing decision end to end at 5,000 decisions/s on 4 vCPU (the consumer-visible SLA, engine + overhead, owned by the Go build); (ii) **20 us p99 in-engine CPU** per decision (the engine's share, before I/O). What a design-phase repo can measure today is the algorithm on the hot path - the shipped TS Router's `decide()` over the committed window - in CPython.

Method: a full drive of the shipped policy on baseline-steady-v1@sha256:b49193b7e715 over the run window (n=60,000 on the committed profile); per decision, `time.perf_counter_ns()` brackets `router.decide()` ONLY (posterior updates and world answers are outside the bracket; the constraint filter and the score-EV sampling are inside, because production pays them per decision). Wall lines are host-dependent and masked with `<!-- @timing -->`: `--check` compares everything except them.

| quantity | p50 | p99 | p99.9 | window | status |
|---|---|---|---|---|---|
| in-engine decision, CPython 3.11 reference Router (us) | 107.6 | 196.0 | 246.3 | 60,000 decisions | MEASURED <!-- @timing --> |
| ...projected to the native band at ADR-0001's 10-30x (us) | 3.6-10.8 | 6.5-19.6 | - | same | MODEL (interpreter-dominated numeric loop; ADR-0001 section 1) <!-- @timing --> |
| in-engine CPU budget (ADR-0001): 20 us p99 | - | 20.0 | - | - | MODEL-BOUND: projected band 6.5-19.6 us is the claim, not the measurement <!-- @timing --> |
| Go engine decide() p99, 5k d/s, 4 vCPU | - | PENDING | - | - | PENDING: no Go toolchain; the gate is armed via `--go-bench` and must read <= 20 us p99 (in-engine) / <= 2 ms p99 (wall) |

On this host the CPython reference pays p99 196.0 us per decision against the 2 ms p99 wall budget, and the MODEL band at ADR-0001's 10-30x is 6.5-19.6 us against the 20 us p99 in-engine budget <!-- @timing --> - what a loaded host does to these rows is visible here and gated nowhere (R121). Wall-clock rows in this section are marked <!-- @timing --> and are never digested.

## B3 - oracle regret (Z): how much margin is left on the table

Three margins per scenario, one world, one execution loop:
  * **shipped**: the learned policy (reused from [B1]'s run, so the regret is never spread across two different runs);
  * **oracle**: the shipped score functional fed TRUE rates (theta from the harness's attempt evaluation, P(timeout) closed-form) - knows today's rates, not tonight's outcomes;
  * **clairvoyant**: per transaction, the chain with the best REALIZED margin over all arms and both attempt slots, computed by probing the index-addressed world (an unattainable bound: it knows the draws).

Z = (oracle - shipped) / oracle, full horizon and post-warmup (first 10,000 transactions excluded); the clairvoyant gap = (clairvoyant - oracle) / clairvoyant prices the score functional itself, so a small Z cannot hide behind a weak oracle.

### baseline-steady-v1@sha256:b49193b7e715  (n=60,000)

| policy | margin c/1k | margin c/1k post-warmup |
|---|---|---|
| shipped TS | 27,376.2 | 27,319.3 |
| oracle (true rates, shipped score) | 28,219.6 | 28,201.8 |
| clairvoyant (knows the draws; bound) | 33,287.6 | - |

**Z = 2.99%** regret full horizon, **3.13%** post-warmup; **clairvoyant gap 15.22%**.

### black-friday-degraded-v1@sha256:89c669ee7371  (n=60,000)

| policy | margin c/1k | margin c/1k post-warmup |
|---|---|---|
| shipped TS | 25,305.8 | 24,884.8 |
| oracle (true rates, shipped score) | 26,775.1 | 26,485.8 |
| clairvoyant (knows the draws; bound) | 32,815.2 | - |

**Z = 5.49%** regret full horizon, **6.04%** post-warmup; **clairvoyant gap 18.41%**.

### outage-recovery-v1@sha256:1e0cd5cd764e  (n=60,000)

| policy | margin c/1k | margin c/1k post-warmup |
|---|---|---|
| shipped TS | 27,353.1 | 27,291.5 |
| oracle (true rates, shipped score) | 28,213.8 | 28,194.9 |
| clairvoyant (knows the draws; bound) | 33,279.6 | - |

**Z = 3.05%** regret full horizon, **3.20%** post-warmup; **clairvoyant gap 15.22%**.

Nothing in this section can be tuned after the fact: the shipped rows are bit-identical to [B1]'s, and the oracle is a definition, not a fit - it receives exactly the information the ticket was promised (true rates), through the same gate the shipped policy uses.

## B4 - off-policy evaluation: IPS-clip10 against replayed truth

The dashboard's counterfactual question, `shift(target, share=rho, window)`, answered by the shipped estimator (IPS, clipped at 10, with exact quadrature propensities recomputed from the logged posteriors) and graded against the replayed ground truth of spikes/0015's protocol on re-seeded worlds.

Definitions and gate (ADR-0015 [O5], restated as this section's contract): at N >= 30,000 decisions in the window, a query PASSES iff (i) support >= 1,000 logged head==target decisions, else the query is REFUSED before any estimator runs, and (ii) |estimate - truth| <= max(1,500 c/1k, 75% of |truth - baseline|). Estimates use the Shipped policy's own decision log; 'truth' replays the overlay through the SAME world instance, so the pairing is exact, not sampled.

Window: 30,000 decisions of baseline-steady-v1@sha256:b49193b7e715 per world; targets ('foxtrot', 'charlie') at rho=0.2; world seeds the scenario seed plus offsets (0, 10000, 20000, 30000) (`seed + off`, OPE_SEED = 20260920 for the overlay coin).

| target | world seed | head rows | truth c/1k | IPS-clip10 c/1k | err c/1k | verdict |
|---|---|---|---|---|---|---|
| foxtrot | +0 | 1980 | 25,828.6 | 24,551.2 | -1,277.4 | PASS |
| charlie | +0 | 13065 | 27,459.6 | 27,426.9 | -32.7 | PASS |
| foxtrot | +10,000 | 1981 | 25,145.4 | 24,105.7 | -1,039.7 | PASS |
| charlie | +10,000 | 13426 | 26,975.5 | 27,347.6 | +372.1 | PASS |
| foxtrot | +20,000 | 1987 | 25,718.8 | 24,677.8 | -1,041.0 | PASS |
| charlie | +20,000 | 13247 | 27,402.6 | 27,367.2 | -35.4 | PASS |
| foxtrot | +30,000 | 2241 | 25,812.8 | 24,661.6 | -1,151.2 | PASS |
| charlie | +30,000 | 13056 | 27,441.4 | 27,430.0 | -11.4 | PASS |

Error floor across the 8 support-bearing queries: mean -527.1 c/1k, RMS 811.1 c/1k - this spread is the finest error the protocol can testify about, and it ships in the file rather than behind a point estimate.

The gate REFUSES a query with no support rather than answering confidently from nothing, and the unclipped-IPS catastrophe from spike-0015 [O5] (+9.1M c/1k on a single thin-propensity row) is why the shipped estimator is the clipped one. Both are properties of the gate, not options a querying user can toggle.

## B5 - determinism: the run may not depend on HOW it is run

Two digest identities, restated per run so this file is falsifiable: (i) the WORLD's answer to every (transaction, arm) first attempt - policy-free by construction; (ii) the shipped policy's LEARNED STATE after the full window (its five float64 posterior arrays, packed and hashed). `##B == ##A` rows must be bit-identical or the run fails.

world digest, baseline-steady-v1@sha256:b49193b7e715, arm-scan n=20,000:
  `sha256:0a60ead7735500758e8752db26c09e41764656687966f0c269ad03215e7dbe46`
  re-run in this process: IDENTICAL

policy-state digest, shipped TS over baseline-steady-v1@sha256:b49193b7e715, n=60,000:
  `sha256:0a09b7940e69fd6e84b6e335`
  re-run in this process: IDENTICAL
  fresh interpreter (PYTHONHASHSEED=random, TZ=Asia/Kolkata): `sha256:0a09b7940e69fd6e84b6e335` -> IDENTICAL

The fresh-interpreter row is the load-bearing one: hash seeds and timezones are the usual ways a run quietly becomes a different run, and neither may touch a benchmark file. `PYTHONHASHSEED=random` is the hostile case, not the friendly one.

## summary verdicts

| section | check | verdict | detail |
|---|---|---|---|
| B0 | catalog pin recompute | PASS | sha256:78b918123635 == sha256:78b918123635 |
| B0 | determinism-contract vectors | PASS | 25-line splitmix64 |
| B0 | golden hash pins | PASS | 5 documents pinned |
| B1 | driver anchor vs spikes/0015 (27,376.2 c/1k, 70.49% auth) | PASS | 27,376.2 c/1k, 70.49% |
| B1 | MDR-only baseline reproduces its committed value (14,787.7 c/1k) | PASS | 14,787.7 c/1k |
| B1 | frontier sanity (baseline-steady-v1) | PASS | TS margin-dominates all 3 static rows; vs MDR-only +16.17 pts / x1.85; vs best static +19.07 pts / x1.04 |
| B1 | frontier sanity (black-friday-degraded-v1) | PASS | TS margin-dominates all 3 static rows; vs MDR-only +16.97 pts / x2.30; vs best static +20.66 pts / x1.07 |
| B1 | frontier sanity (outage-recovery-v1) | PASS | TS margin-dominates all 3 static rows; vs MDR-only +16.12 pts / x1.85; vs best static +19.11 pts / x1.04 |
| B2 | Go engine latency gate | PENDING | no Go toolchain in this sandbox (ADR-0001 day-one); arm with --go-bench when it exists |
| B2 | latency quantile ordering (p50<=p99<=p99.9) | PASS | structured check, value-free |
| B2 | host-dependent rows masked per R121 | PASS | 4 B2 rows carry the mask; host load cannot move a verdict |
| B3 | regret ordering (baseline-steady-v1) | PASS | shipped <= oracle <= clairvoyant; Z=2.99% full, 3.13% post-warmup, gap 15.22% |
| B3 | regret ordering (black-friday-degraded-v1) | PASS | shipped <= oracle <= clairvoyant; Z=5.49% full, 6.04% post-warmup, gap 18.41% |
| B3 | regret ordering (outage-recovery-v1) | PASS | shipped <= oracle <= clairvoyant; Z=3.05% full, 3.20% post-warmup, gap 15.22% |
| B4 | shift(foxtrot,0.2) world +0 | PASS | |err| 1,277.4 <= 1,500.0 (delta 1,645.6) |
| B4 | shift(charlie,0.2) world +0 | PASS | |err| 32.7 <= 1,500.0 (delta 14.6) |
| B4 | shift(foxtrot,0.2) world +10000 | PASS | |err| 1,039.7 <= 1,500.0 (delta 1,797.9) |
| B4 | shift(charlie,0.2) world +10000 | PASS | |err| 372.1 <= 1,500.0 (delta 32.2) |
| B4 | shift(foxtrot,0.2) world +20000 | PASS | |err| 1,041.0 <= 1,500.0 (delta 1,629.3) |
| B4 | shift(charlie,0.2) world +20000 | PASS | |err| 35.4 <= 1,500.0 (delta 54.5) |
| B4 | shift(foxtrot,0.2) world +30000 | PASS | |err| 1,151.2 <= 1,500.0 (delta 1,582.5) |
| B4 | shift(charlie,0.2) world +30000 | PASS | |err| 11.4 <= 1,500.0 (delta 46.1) |
| B4 | error floor stated | PASS | mean -527.1, RMS 811.1 c/1k over 8 queries |
| B5 | world digest stable across in-process re-run | PASS | sha256:0a60ead7735500758e87 |
| B5 | policy-state digest stable (in-process x2 + fresh interpreter) | PASS | sha256:0a09b7940e69fd6e84b6e335 |

**24 PASS / 1 PENDING (gate armed) / 0 FAIL** - exit status reflects FAIL rows only; PENDING means the thing measured does not exist yet, and the row says what arms the gate.

